language: cpp
dist: hirsute
compiler: gcc

addons:
  apt:
    packages:
      - cmake

script:
  - git submodule foreach git pull
  - mkdir -p build && cd build
  - cmake ..
  - cmake --build .
  - ./tests
